@using ru_football.Models;
@model CalculateTourResultModel

@{
    ViewBag.Title = "Add new match result";
}

@using (Html.BeginForm())
{
    <label>Номера матчей:</label> @Html.TextBoxFor(x => x.Numbers)
    <br />

    <label>
        Туры:
    </label>
     @Html.TextBoxFor(x => x.Tours)
        <br />
        <input type="submit" value="submit" />
        }

        <textarea>
            @Model.Result
        </textarea>

        @Html.Raw(Model.Result)
